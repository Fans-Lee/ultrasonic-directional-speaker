# 05 · 实施计划、人员分工与集成规则

> 本文件把项目拆成可以并行推进、可以单独验收的工作包。
> 示例按 3 人团队编排：A 负责软件/算法，B 负责电路/电源，C 负责阵面/测试/文档。具体姓名可替换，但模块接口和验收责任不变。

---

## 1. 项目管理原则

1. **按门验收，不按日期假装完成**：G1 没通过就不能跳到 G3；
2. **先保底，再完整，再进阶**：摄像头和 16 通道不能抢占单通道调试时间；
3. **一个变量原则**：一次只改供电、负载、电感、频率、固件中的一个；
4. **接口先冻结**：列编号、连接器、GPIO、协议和坐标系先写文档，再焊线/写代码；
5. **证据交付**：完成的定义是“实物/代码 + 测量 + 记录”，不是“我做过了”；
6. **可回退**：每个阶段保留能工作的固件 tag、线束和配置；
7. **功能冻结**：答辩前最后一周不新增硬件架构和高风险功能。

---

## 2. 工作包总表

| 工作包 | 内容 | 负责人 | 上游输入 | 交付物 | 独立验收 |
|---|---|---|---|---|---|
| WP0 需求/配置 | 三版本范围、坐标、编号、接口冻结 | A+B+C | 实物尺寸、目标 | 配置和接口基线 | 三人签字/评审 |
| WP1 声学/仿真 | 主瓣、栅瓣、焦区、相位表 | A | 阵元坐标 | 脚本、图、假设 | 与解析式/Ultraino交叉验证 |
| WP2 阵面 | 16×6 安装、16 列独立母线 | C，B复核 | 换能器和布局 | 阵面、列电容表 | 通断、隔离、极性、照片 |
| WP3 单通道驱动 | TC4428A、去耦、差分、旁路 | B | GPIO 测试信号 | 1 路驱动样板 | 示波器和温升 |
| WP4 电源/安全 | 适配器、保险丝、开关、星形分配 | B，C复核 | 12 V 负载预算 | 电源模块/接线图 | 极性、短路、压降、急停 |
| WP5 8 路驱动 | 复制 8 路、逻辑缓冲、连接器 | B | WP3 通过 | 驱动板、8→16线束 | 8 路逐路波形 |
| WP6 ESP32 固件 | 载波、音频、相位、协议、静音 | A | GPIO/协议基线 | 固件和测试 | 逻辑波形、欠载和故障测试 |
| WP7 PC 控制/视觉 | 手工控制、录像/摄像头目标 | A，可由第三人协助 | 协议 | PC 程序 | mock 串口/录像回放 |
| WP8 声场测量 | 方向图、频响、THD、焦区 | C，A处理数据 | 整机和测试信号 | 原始数据、图表 | 可重复测量 |
| WP9 集成/答辩 | 装配、演示、PPT、报告、备份 | C牵头，全员 | 全部交付物 | 黄金版本和答辩包 | 完整彩排 |

---

## 3. 每个人具体做什么

### 3.1 A：嵌入式、算法和 PC 控制

#### 必须交付

1. `array_geometry.json` 和 8/16 通道分组配置；
2. 40 kHz 8 路测试固件、默认静音和状态机；
3. 测试音、内置语音、DSB 调制；
4. 文本协议和自动解析测试；
5. 8 路静态相位设置和 `FIXED/STEER/FOCUS`；
6. 初级声场仿真、相位表和测量绘图脚本；
7. PC 手工控制工具；
8. 若主链路完成，再做 USB 摄像头目标插件；
9. 固件构建说明、二进制、tag 和已知问题。

#### 可以在没有整机时完成

- 协议、状态机、音频预处理、LUT；
- 几何/相位单元测试；
- 仿真和图表；
- PC UI 和 mock 设备；
- 对录像文件做视觉检测。

#### 必须向 B/C 请求的证据

- 8 路 GPIO 波形原始截图/逻辑分析文件；
- TC4428A OUTA/OUTB 和差分波形；
- 不同占空比下的边沿和相位；
- 全阵方向图和焦区原始 CSV；
- 故障静音的视频/波形。

### 3.2 B：电路、电源和驱动

#### 必须交付

1. 适配器、DC5521、保险丝、开关和星形配电；
2. ESP32 GPIO/排针审计；
3. 一路 TC4428A 驱动样板；
4. 6 个和 12 个负载的电容、扫频、Vpp、电流和温升；
5. 8 路洞洞板驱动和 AHCT 缓冲；
6. 可旁路/可更换的电感位置；
7. 8→16 可拆配线；
8. 驱动板接线图、测试点图和最大已验证设置；
9. 故障备件和快速更换方法。

#### 不得自行变更

- 列编号和 A/B 定义；
- 8 通道映射；
- GPIO 表和逻辑接口；
- 供电超过已验证值；
- 批量电感值。

需要变更时先发 issue，说明原因、影响和回退方式，A/C 同意后更新接口基线。

### 3.3 C：阵面、测试、采购和文档

#### 必须交付

1. 库存/物流/批次登记；
2. 换能器尺寸、电容、频率和极性抽测；
3. 1:1 阵面纸样和 16×6 洞洞板阵面；
4. 16 列通断、电容、照片和编号；
5. 测试场地、距离/角度标记、麦克风固定；
6. 方向图、频响、THD、SPL 趋势和热力图原始数据；
7. 每次实验的配置、电压、电感、固件版本记录；
8. 演示脚本、报告、PPT、实物标签和视频；
9. 安全检查表和现场回退预案。

#### 测试纪律

- 不用手机不同时间自动增益后的数值拼成精确方向图；
- 不只记录“听起来更响”，要有信号、距离、角度、仪器和原始文件；
- 不删除“不好看”的数据，异常点标记后复测；
- 可听声测量和 40 kHz 超声测量使用适合各自频段的仪器；
- 每张答辩图都能追溯到 CSV/音频和配置版本。

---

## 4. RACI 责任矩阵

`R`=执行，`A`=最终负责，`C`=需会签，`I`=知会。

| 决策/交付 | A 软件 | B 硬件 | C 测试/文档 |
|---|---:|---:|---:|
| 三版本范围 | C | C | A |
| 坐标和通道映射 | A/R | C | C |
| GPIO 冻结 | C | A/R | I |
| 单通道电路 | C | A/R | C |
| 电源和安全 | I | A/R | C |
| 阵面施工 | C | C | A/R |
| 固件架构 | A/R | C | I |
| 串口协议 | A/R | C | C |
| 视觉是否进入完整版 | R | C | A |
| 测量方案 | C | C | A/R |
| 最大演示功率 | C | R | A/C |
| 版本冻结/发布 | R | R | A |

任何安全相关否决不遵循多数投票：发现短路、过压、过热、超声暴露或机械风险的人均有权立即停机。

---

## 5. 阶段门和交付关系

### G0：实物和接口冻结

**A**：创建配置模板和列坐标格式。
**B**：核对 TC4428A、ESP32 引脚、电源接口。
**C**：测换能器尺寸/电容，完成纸样。

通过材料：

- `hardware_pins.json`；
- 16 列坐标；
- 到货抽测表；
- 单通道接线图；
- 适配器极性照片。

### G1：单换能器闭环

**A**交付单路 40 kHz/静音固件；**B**交付 TC4428A 样板；**C**记录波形和温升。

通过后保留一套不拆的“黄金单通道”，后续全阵出问题时用于对比。

### G2：一列 6 个

**C**只焊一列；**B**测负载变化；**A**不改相位算法。

通过材料：列电容、波形、电流、换能器方向一致性记录。

### G3：当前一个完整通道（两列 12 个）

在电感旁路下先通过，再选择电感样品扫频。只有 G3 得到稳定值后，才批量准备其余 7 路。

### G4：8 路时序和8路驱动

**A**提供固定相位测试表；**B**逐路装配；**C**按同一触发条件保存 8 路证据。

这一步不接完整阵面或逐路接已知负载，避免 8 个未知问题叠加。

### G5：96 阵元保底版

全阵先同相、固定正前方。完成：

- 轴上可辨认语音；
- 完整方向图初测；
- 10～15 分钟温升；
- 电源压降和保险丝/急停；
- 不接 PC 的独立演示。

G5 即为保底版冻结点，建立 `baseline-v1` tag 和线束照片。

### G6：8 路相位完整版核心

**A**加入 `STEER/FOCUS`；**B**确认驱动无变化；**C**测峰值移动和二维焦区。

若相位后端失败，回到 G5，不拆阵面、不动电感，同时单独研究后备时序。

### G7：PC/视觉插件

先手工 UI，再录像，再实时摄像头。视觉断开不影响 G6。只有在 G6 稳定且现场确实需要时，视觉才计入完整版。

### G8：16 通道进阶

拆换线束、增加驱动、改时序、重算电感、重跑 G4～G7。G8 是独立升级项目，不放进保底/完整版关键路径。

---

## 6. 四周参考节奏

实际日期从器件到货和 G0 通过之日计算。

| 周 | 共同目标 | A | B | C |
|---|---|---|---|---|
| 第1周 | G0～G2 | 仿真/协议/单路固件 | 电源/单驱动 | 验货/纸样/一列阵面 |
| 第2周 | G3～G5 | 内置音频/8路同相/状态机 | 8路驱动/调谐 | 全阵焊接/方向图初测 |
| 第3周 | G6 | 相位/转向/焦点/手工PC | 波形复测/标定支持 | 热力图/频响/THD |
| 第4周 | G7或打磨 | 可选视觉/修复 | 外观/备件/稳定性 | 报告/PPT/视频/彩排 |

进阶版 G8 不强塞进四周；只有第三周中段完整版核心已经通过，才评估是否启动。

---

## 7. 并行工作怎样真正解耦

### 7.1 A 不等阵面

- 用 GPIO/逻辑分析仪验证 8 路；
- 用 mock 配置生成相位；
- 用虚拟串口开发 PC 工具；
- 用仿真数据开发热力图；
- 用预录视频开发视觉。

### 7.2 B 不等完整固件

- 使用函数发生器或 A 的固定 40 kHz 测试固件；
- 先验证 TC4428A 电平、去耦和差分输出；
- 使用电容/小批换能器逐级加负载；
- 8 路驱动以相同测试输入逐个复制。

### 7.3 C 不等驱动板

- 完成尺寸、电容、批次筛选；
- 画纸样、焊一列、建立编号；
- 搭测量支架和距离/角度标记；
- 准备测试信号、数据表和答辩结构；
- 用普通扬声器/固定录音先验证采集脚本流程。

---

## 8. 变更控制

下面内容属于“接口基线”，未经记录不得口头修改：

- 坐标方向和单位；
- `COL0～COL15` 的物理左右顺序；
- A/B 极性和连接器方向；
- 8 通道配对映射；
- GPIO 和逻辑连接器；
- 串口协议版本；
- 电源额定、保险丝和最大已验证增益；
- 测量距离、角度正方向和归一化方法。

变更 issue 模板：

```text
标题：Change <模块> <一句话>
原因：
当前基线：
拟修改：
影响模块：
验证方法：
回退方法：
需要会签：A/B/C
```

紧急安全停机可以先执行，恢复工作前补记录。

---

## 9. 协作和证据规则

### 9.1 每日短同步

每人回答四件事：

1. 昨天通过了哪一个可验证项；
2. 证据链接在哪里；
3. 今天准备改变哪一个变量；
4. 是否需要另一个角色提供输入。

不汇报“做了很多”“大概能用”。

### 9.2 文件命名

```text
YYYYMMDD_G3_CH0_12V_bypass_OUTA.png
YYYYMMDD_G3_CH0_12V_560uH_DIFF.png
YYYYMMDD_G5_polar_2m_1kHz.csv
YYYYMMDD_G6_focus_x0_z1000_gain030.csv
```

文件旁必须有元数据：固件 commit、配置哈希、电源、电感、距离、仪器、操作者。

### 9.3 交付和验收

```text
负责人提交代码/实物/文档
  → 指定验收人按书面步骤复测
  → 证据进入 docs/measurements
  → 通过：合并/打 tag/进入下一门
  → 不通过：保留失败数据，回退并新建问题
```

代码作者不能单独宣布自己的硬件相关功能已经完成；至少需要另一个人在实物上复测。

---

## 10. 三块 ESP32-S3 的分配

| 板 | 当前用途 | 不做什么 |
|---|---|---|
| #1 | 保底/完整版音频与 8 路相位主控 | 不跑摄像头和 Wi‑Fi 图传 |
| #2 | 固件开发、逻辑时序实验、备用烧录 | 不接完整大电流阵面 |
| #3 | 完整备件；进阶版可研究 16 路/辅助控制 | 保底版不强制使用 |

PC 负责可选视觉。这样任何一块实验板出问题都不会把唯一主控和全部进度锁死。

---

## 11. 功能冻结和答辩准备

建议在正式答辩前 5～7 天冻结：

1. `git tag demo-freeze-v1`；
2. 保存可直接烧录的固件；
3. 保存 `golden_demo.json`；
4. 给线束、适配器、通道和开关贴标签；
5. 连续运行并完成完整演示录像；
6. 复制源码、数据、PPT、视频到至少两种介质；
7. 之后只修阻断演示的 bug；
8. 任何修复都能一键回滚到冻结版。

答辩主流程使用已通过的最低稳定版本。未通过的视觉或 16 通道只能放在“进行中/未来工作”，不能现场冒险临时接入。

---

## 12. 完成定义

一个工作包只有同时满足以下条件才算完成：

- 输入和输出接口明确；
- 实物或代码已交付；
- 书面验收步骤可由别人重复；
- 原始证据已归档；
- 已知限制和最大安全设置已写明；
- 失败时有回退版本；
- 对应 README/01～07 没有留下矛盾描述。

采购与到货门见 [06-采购清单.md](06-采购清单.md)，每个版本的验收、风险和答辩规则见 [07-版本阶梯与风险.md](07-版本阶梯与风险.md)。
