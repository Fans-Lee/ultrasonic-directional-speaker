# 07 · 三级验收、测试、风险、安全与答辩

> 本文件是项目的“完成定义”。
> 任何写入报告、PPT或答辩台词的性能数字，都应能在本文件规定的方法下追溯到原始数据。

---

## 1. 三个版本怎样才算完成

### 1.1 保底版验收

#### 功能范围

- 96 个 TCT40-16T，16 列 × 6 行；
- 16 列独立接出，通过可拆线束组成 8 通道；
- 8 个 TC4428A，先全部同相；
- 一块 ESP32-S3 独立运行；
- 固定测试音和内置语音；
- 无摄像头、无旋转平台、无运行时电脑依赖；
- 电感可旁路，只有 G3 调谐通过才装入演示配置。

#### 必须全部通过

| 类别 | 验收项 | 通过条件/证据 |
|---|---|---|
| 电源 | 保险丝、总开关、星形配电 | 极性正确；开关可立即断电；线束/接口无异常温升 |
| 单通道 | TC4428A互补输出 | OUTA、OUTB和差分波形截图；无异常静态电流 |
| 8路逻辑 | 频率和同相 | 8路均为目标载波；编号与GPIO表一致 |
| 安全 | 默认静音 | 上电、复位、固件异常时不持续切换 |
| 全阵 | 轴上语音可辨认 | 规定距离、规定语句，至少3名听者可复述主要内容 |
| 方向性 | 峰值和侧向差异可重复 | 相同设置下重复扫描，曲线趋势一致；给出-3/-10 dB宽度而非主观描述 |
| 稳定性 | 连续10～15分钟 | 无重启、保险丝熔断、连接器/驱动/电感异常发热 |
| 文档 | 可接手 | 有接线图、固件、配置、BOM、测量原始数据 |

保底版成功的核心是“定向声学链路完整且可测”，不是声音必须达到某个预先想象的商业音量。

### 1.2 完整版验收

在保底版全部通过的基础上增加：

| 类别 | 验收项 | 通过条件/证据 |
|---|---|---|
| 8路相位 | 静态相位可控 | 至少验证0/90/180/270°；记录误差和长时漂移 |
| 动态更新 | 无不可接受毛刺 | 示波器捕获更新前后；相位变化采用静音/渐变策略 |
| 转向 | 峰值按命令移动 | `-θ、0、+θ` 至少3点，峰值方向符号和趋势正确 |
| 聚焦 | `x-z`场发生可解释变化 | 至少两个焦点设置的二维/轴向数据；给出焦区而非“一个点” |
| 协议 | 手工命令全覆盖 | 合法/非法/断线/重连测试通过 |
| PC控制 | 手工界面可用 | 不连接摄像头也能设角度、焦点、音源、增益、静音 |
| 视觉（可选） | 只是目标插件 | 录像和实时摄像头可输出目标；断开后音响仍安全 |
| 声学 | 完整方向图和热力图 | 同时展示主瓣、旁瓣、栅瓣和测量条件 |

完整版本不要求旋转平台。若8通道相位的实际声场没有比固定模式提供可信增益，仍保留保底版作为答辩主实物，把相位结果如实作为实验结论。

### 1.3 进阶版验收

- 16 列解除配对，16 个独立驱动通道；
- 每通道 6 个换能器，电感重新计算和扫频；
- 16 路时序后端通过频率、相位、更新和稳定性测试；
- 每列幅相校准并写入配置；
- 重做方向图、转向和焦区测量，与8路结果并列对比；
- 深度相机、ToF或旋转平台若加入，分别具有独立断开和回退模式；
- 数据证明升级带来可量化改善，否则不能仅以“通道更多”宣称进阶成功。

---

## 2. 项目状态看板

每次周会只更新以下表格：

| 门 | 状态 | 通过日期 | 负责人 | 证据路径 | 下一阻塞 |
|---|---|---|---|---|---|
| G0 器件/接口 | NOT_STARTED | | | | |
| G1 单换能器 | NOT_STARTED | | | | |
| G2 一列6个 | NOT_STARTED | | | | |
| G3 两列12个/调谐 | NOT_STARTED | | | | |
| G4 八路电气 | NOT_STARTED | | | | |
| G5 全阵保底 | NOT_STARTED | | | | |
| G6 八路相位 | NOT_STARTED | | | | |
| G7 PC/视觉 | OPTIONAL | | | | |
| G8 十六通道 | OPTIONAL | | | | |

状态只能是 `NOT_STARTED / IN_PROGRESS / PASSED / FAILED / OPTIONAL`。不能用“差不多”“基本完成”。

---

## 3. 电气测试方法

### 3.1 G0：不上高功率的检查

1. 万用表确认适配器中心正、空载约12 V；
2. 确认保险丝、开关、线径和端子额定；
3. 20只以上换能器测电容，10只以上扫40 kHz附近响应；
4. 试插洞洞板，冻结真实中心坐标；
5. 核对 TC4428A 引脚、缺口、品牌和批次；
6. 核对 ESP32 GPIO 与自制板原理图/排针；
7. 上电前测12 V对地电阻，排除明显短路。

### 3.2 G1：单TC4428A + 单换能器

建议使用可限流实验电源，暂不接谐振电感。

测量点：

- ESP32输入；
- TC4428A OUTA对GND；
- OUTB对GND；
- 数学通道 OUTA-OUTB；
- 12 V电源电流。

记录：频率、单端Vpp、差分Vpp、上升/下降沿、传播延迟、静音状态、5分钟温升。

不得把普通示波器地夹接到任一差分输出。见 [02](02-系统方案与硬件.md) 的测量方法。

### 3.3 G2/G3：逐级加负载

| 步骤 | 负载 | 电感 | 只观察 |
|---|---|---|---|
| 1 | 单个 | 旁路 | 基准波形/电流 |
| 2 | 一列6个 | 旁路 | 负载增加后的变化 |
| 3 | 两列12个 | 旁路 | 当前完整通道是否稳定 |
| 4 | 两列12个 | 第一只试配L | 扫频、Vpp、输入电流、温升 |
| 5 | 同上 | 更换一个L或加阻尼 | 单变量比较 |

每个设置先运行数秒观察，再逐步延长。出现尖峰、波形塌陷、驱动/电感快速升温或适配器保护，立即静音断电，不继续“多试一会”。

### 3.4 G4：8路时序

测试表：

| 测试 | 命令/配置 | 观察 |
|---|---|---|
| 同相 | 全0° | 8路边沿重合程度 |
| 正交 | 0/90/180/270°循环 | 相位符号和量化误差 |
| 边界 | 359°→0° | 是否产生异常长脉冲 |
| 动态 | 0°→指定相位 | 毛刺、不同步周期 |
| 占空比 | 多个包络值 | 脉冲中心、AM-PM耦合 |
| 长时 | 固定相位10～15分钟 | 漂移、丢周期、重启 |
| 故障 | 复位/断串口/欠载 | 是否安全静音 |

逻辑分析仪适合同时看8路数字边沿；模拟输出和谐振必须用示波器。

### 3.5 温升记录

至少记录：

```text
环境温度：
供电电压/输入电流：
音源/增益/调制度：
负载数量/电感/阻尼：
0、1、5、10、15分钟：TC4428A / 电感 / 端子 / 适配器温度
异常气味/声音：
```

最大演示设置应低于出现快速温升、失真突增或供电不稳的设置，并保留工程余量。

---

## 4. 声学测试方法

### 4.1 先控制环境和仪器

- 选择较大、较安静的房间，记录房间尺寸和主要反射面；
- 阵面固定，麦克风高度对准阵面中心；
- 所有比较使用同一麦克风、声卡增益、采样率和位置基准；
- 关闭自动增益、降噪、回声消除和自动曝光类音频处理；
- 手机只能做辅助听感，不作为精确超声仪器；
- 每组测量先录背景噪声；
- 测试信号比音乐更适合：500 Hz、1 kHz、2 kHz单频和规定语句。

### 4.2 固定方向图

不用旋转平台也可以测。把阵面固定在 `z=0`，麦克风在固定距离 `z` 处沿水平 `x` 方向移动：

```text
角度 θ = atan(x/z)
```

建议：

- 距离先选 1 m 和 2 m 中房间反射较小、信噪比足够的一处；
- 主瓣附近以 5～10 mm 步长扫描；
- 远离主瓣后可用 25～50 mm 步长；
- 扫描范围至少覆盖理论栅瓣可能出现的位置；
- 每点保持相同时间，最好做连续录音并用同步标记切段；
- 从录音FFT提取目标可听频率的幅值，归一化到主峰0 dB。

输出图必须同时包含：

- 横轴 `x(mm)` 和换算角度；
- -3 dB、-10 dB线；
- 主瓣、主要旁瓣/栅瓣位置；
- 理论曲线仅作叠加对比并标 `SIM/CALC`；
- 原始CSV和脚本版本。

### 4.3 为什么不同点分开用手机App测不可靠

手机系统常有自动增益和频响修正。把手机移到一个点读一次、再到另一个点读一次，增益可能已经改变。更可取的是：

- 使用可关闭AGC的外置声卡；或
- 固定录音增益，连续采集整个扫描；
- 用相同时间窗的1 kHz FFT幅值做相对比较。

绝对SPL只有在麦克风和测量链经过校准时才报告；否则写“相对声压级”。

### 4.4 频率响应

位置固定在轴上，使用相同可听目标电平/调制设置，至少测：

```text
300, 500, 800, 1000, 1500, 2000, 3000, 4000, 5000 Hz
```

若换能器带宽太窄，高频或低频可能接近噪声底。图上保留噪声底和不可用点，不做过度平滑。频率响应决定最终演示语音的滤波范围。

### 4.5 THD和互调

THD测试：

```text
THD = sqrt(V2²+V3²+...)/V1
```

至少比较：

- DSB与SRAM；
- 多个调制度；
- 相同目标基波声压，而不是一个方案更响；
- 500 Hz、1 kHz等换能器能有效重放的频点。

强40 kHz超声可能让麦克风、前置放大器或ADC自身产生互调，制造“假的可听声/失真”。测量链应有足够带宽和线性，必要时在采集前使用合适的低通，做空白/遮挡/不同距离对照。参考 [调制实验论文](https://doi.org/10.3390/app10062070) 的测量思路。

### 4.6 转向测试

对 `ANGLE -θ、0、+θ`：

1. 每个命令测完整横向曲线；
2. 读取全局最大峰，而不只在预期位置附近找局部峰；
3. 记录主瓣峰值变化、峰值角度、最大非目标峰；
4. 逐步从 ±2° 到 ±5°；
5. 当非目标峰接近主峰或主峰明显下降时，停止扩大范围；
6. 最终UI角度限制采用实测可用范围。

### 4.7 聚焦测试

至少测 `x-z` 网格：

- `x` 覆盖中心左右；
- `z` 覆盖预定演示距离前后；
- 比较 `FIXED` 与多个 `FOCUS(x,0,z)`；
- 记录目标点增益、横向-3/-10 dB宽度、轴向焦深、最大旁峰；
- 若只看到峰值方向改变而没有距离选择性，结论应写“转向有效，轴向聚焦未证实”。

普通听者在几个点站立不能替代二维扫描。

---

## 5. 超声暴露和安全

### 5.1 安全立场

40 kHz 超过多数人的听觉上限，但“听不到”不等于“无需管理”。[ICNIRP](https://www.icnirp.org/en/frequencies/ultrasound/index.html)指出空气超声可能伴随高频可听成分和不适报告，现有证据与指南仍有不确定性；[Health Canada Safety Code 24](https://www.canada.ca/en/health-canada/services/environmental-workplace-health/reports-publications/radiation/guidelines-safe-use-ultrasound-part-industrial-commercial-applications-safety-code-24.html)强调按频带、在人员耳位使用合适仪器测量并采取保守防护。

本项目采用“尽量降低且足以完成实验”的原则，不自行宣布设备绝对安全。

### 5.2 强制规则

1. 工程调试时不把人或动物作为声压探头；
2. 阵面正前方近距离区域设置警戒，不把头贴近换能器；
3. 从低电压、低调制度和短时间开始；
4. 无需输出时立即静音，装配/插拔前断电；
5. 上电默认静音，故障、目标丢失和PC断连按配置静音；
6. 保留能立即切断12 V的物理总开关；
7. 演示前在人员耳位测40 kHz及邻近1/3倍频程，仪器必须适合该频段；
8. 根据学校实验室安全要求和选定指南评估，不用A计权可听噪声读数替代40 kHz测量；
9. 任何人出现耳鸣、头痛、恶心或不适，立即停止暴露并离场；必要时寻求专业医疗/安全人员意见；
10. 现场主动告知参与者，可拒绝体验；不对婴幼儿或植入/佩戴听力设备的人进行演示。

耳塞对40 kHz的衰减未知，不能替代降低功率、增加距离、缩短时间、屏蔽和静音联锁等工程控制。

### 5.3 电气规则

- 12 V/5 A短路足以烧毁细线，主回路必须有5 A保险丝；
- 谐振后换能器端Vpp可能高于供电，断电后再触碰；
- 差分输出按正确探头方法测量；
- 电解电容、电源极性和线径每次改线后复查；
- 无人值守时断电；
- 不使用面包板/杜邦线承载最终主电流和换能器输出。

---

## 6. 风险登记表

概率/影响用 H/M/L，负责人每周更新状态。

| ID | 风险 | 概率 | 影响 | 预防 | 触发后的回退 |
|---|---|---:|---:|---|---|
| R01 | 换能器尺寸/引脚距不符 | M | H | 到货实测、纸样试插 | 改洞洞板孔位，不焊满 |
| R02 | 廉价换能器批次离散 | H | M/H | 110只抽测、隔离离群件 | 降低阵元数/换批次/按列配对 |
| R03 | TC4428A来源或性能异常 | M | H | 单芯片限流抽测、留备件 | 更换供应商，不复制8路 |
| R04 | 3.3 V逻辑受干扰 | M | M | 单通道验证、AHCT缓冲、共地 | 加2×AHCT125/缩短信号线 |
| R05 | 12个并联负载过大 | M | H | 1→6→12逐级测试 | 降供电/加阻尼/改6个每路 |
| R06 | 谐振过压/过热 | M | H | 默认旁路、扫频、监测Vpp/温升 | 拔L、加R、降电压/增益 |
| R07 | 主电流走开发板导致过热 | M | H | 外置星形配电和粗线 | 立即改线并检查板损伤 |
| R08 | LEDC更新毛刺/不同步 | M | H | G4早测、后端抽象 | 回到同相保底；研究替代后端 |
| R09 | PWM改变占空比引入相位偏移 | H | M/H | 中心对齐、AM-PM实测 | 查表补偿或改调制后端 |
| R10 | 音频太小或不可懂 | M/H | H | 窄带语音、频响和调制测试 | 降低目标、优化调谐/素材、保留单频展示 |
| R11 | 方向图被房间反射污染 | H | M | 大房间、近距离、连续扫描 | 换场地/加吸声/时间窗测量 |
| R12 | 8路转向产生强非目标峰 | H | M/H | 小角度、完整方向图 | 限制转向；主演示用固定模式 |
| R13 | 聚焦不明显 | H | M | 先仿真、测x-z网格 | 如实降级为转向实验 |
| R14 | 摄像头不能给深度 | H | M | 固定z/手工输入 | 取消动态焦点或加深度传感 |
| R15 | 视觉拖慢/崩溃 | M | L/M | PC插件、与ESP32解耦 | 手工控制/固定模式 |
| R16 | 超声暴露未量化 | M | H | 合适仪器、低功率和距离控制 | 停止对人演示，只展示数据/视频 |
| R17 | 洞洞板串扰/虚焊 | M | M/H | 模块化、逐列测试、去耦 | 按列/通道隔离排查 |
| R18 | 最后一周改坏稳定版 | M | H | 功能冻结、tag、二进制备份 | 回滚黄金版本 |

---

## 7. 故障排查树

### 7.1 完全没有可听声

按顺序检查，前一步不正常就不进入下一步：

1. **音源**：TEST包络是否在变化，欠载计数是否为0；
2. **GPIO**：是否有40 kHz；
3. **TC4428输入**：3.3/5 V逻辑是否到达；
4. **OUTA/B**：是否互补；
5. **差分输出**：是否有预期Vpp；
6. **负载**：列电容、通断和接线A/B；
7. **频率**：换能器本批最佳频率是否偏离40 kHz；
8. **电感**：先旁路，排除调谐错误；
9. **位置**：在轴上规定距离用麦克风测目标频率；
10. **调制**：先单频DSB，排除语音/LUT问题。

### 7.2 有声但很小

| 检查 | 可能原因 | 动作 |
|---|---|---|
| 单通道也小 | 频率/电压/器件问题 | 扫频、查差分Vpp、换参考器件 |
| 单通道正常、全阵小 | 极性/相位/映射错误 | 全0相位、逐列拔插比较 |
| 加电感反而小 | 谐振点错或电感损耗大 | 旁路并换值/测DCR |
| 电源电压下降 | 线细、接口/适配器限流 | 测各节点压降和温升 |
| 只有音乐差 | 带宽/低频内容过多 | 用单频和窄带语音对照 |

不因声音小就直接把电源提高到15～18 V。先确定原因和连续额定。

### 7.3 失真、噪声或咔哒

- 用 TEST单频区分音频源和硬件；
- 查看欠载计数和看门狗日志；
- 检查相位/占空比更新是否有毛刺；
- 检查电感可闻啸叫和机械松动；
- 关闭PC视觉并使用本地语音；
- 比较DSB和SRAM，但保持同一基波声压；
- 降调制度和增益；
- 检查谐振是否过窄导致包络拖尾；
- 检查麦克风/ADC是否被40 kHz过载，排除测量链自产失真。

### 7.4 方向不对或主瓣分裂

1. 设置全0相位，确认固定模式；
2. 核对 `COL0` 是否真在软件定义的左侧；
3. 核对8路配线和A/B；
4. 逐列只开/只拔，找反相或失效列；
5. 测8路相位实际符号；
6. 完整扫描，确认看到的是全局最大峰；
7. 将阵元方向图和真实分组加入仿真；
8. 限制电子转向或回到固定模式。

### 7.5 驱动或电感发热

立即静音并断电，然后检查：

- 实际负载是否超过12个；
- 电感是否饱和/DCR过高；
- 谐振是否造成过高环流；
- 输入是否停在高频异常振荡；
- 去耦是否靠近芯片；
- 端子和焊点是否接触不良；
- 供电电压、频率和占空比是否超出黄金配置。

不能以“手还能摸”为唯一温度判据。

---

## 8. 答辩时如何准确表述

### 8.1 推荐的核心叙述

> 我们用低成本40 kHz空气耦合换能器构成96阵元参量阵。音频先调制到超声，空气的非线性生成定向可听差频。工程上我们把阵面、8/16路驱动、ESP32实时控制和PC视觉彻底解耦：最小系统不需要摄像头和旋转平台，也能独立定向发声；完整系统再验证8路相位转向和有限焦区；进阶系统才升级16路和深度/机械插件。所有性能以方向图、热力图和电气测量为证据。

### 8.2 常见问题

| 评委问题 | 回答要点 |
|---|---|
| 为什么声音有方向性？ | 40 kHz波长约8.6 mm，小口径即可形成窄超声束；空气非线性沿束生成可听差频 |
| 一个通道是什么？ | 一组共享同一相位时序和一个TC4428A的负载；当前8路每路两列=12个，不是一个换能器 |
| 8通道就是96个吗？ | 物理上96个；16列独立，保底/完整时两列合为1通道，共8路 |
| 能不能定点发声？ | 能研究有限的x-z焦区，但不是数学点；8路没有垂直自由度，结果以热力图为准 |
| 和方向发射差在哪？ | 转向用近似线性相位控制方向；聚焦按到有限目标的距离差使用球面相位 |
| 每个换能器要旋转吗？ | 不需要，全部固定朝前，通过电信号相位形成波前 |
| 栅瓣怎么避免？ | 16 mm器件间距远大于λ/2，不能彻底避免；限制转向、利用阵元方向图、实测全角度，必要时整阵机械粗转 |
| 为什么不用FPGA？ | 8路目标与ESP32-S3的外设规模匹配；先测LEDC，软件后端可替换。16路若需更强时序再评估，不提前增加学习成本 |
| 为什么视觉放电脑？ | 视觉不是发声必需；PC更易调试、可录像复现，也不会干扰实时载波；断开后可手工控制 |
| 为什么不用PCB？ | 洞洞板足以验证原理，且可拆列和驱动便于返工；参数稳定后PCB是工程化选项，不是可行性前提 |
| 最大电压多少？ | 不用商品脉冲上限回答连续额定；从12 V供电、无谐振开始，连续设置由Vpp/电流/温升实测确定 |
| 音质为什么不像普通音箱？ | 低成本40 kHz换能器带宽窄，且参量解调有固有频率响应和失真；定位是语音定向，不是Hi-Fi |
| -3 dB是不是听觉阈值？ | 不是；它是相对峰值的工程宽度，另外报告-10 dB和语音可懂区域 |
| 安全吗？ | 听不到不代表零风险；低功率、距离/时间控制、故障静音和耳位超声测量，未完成安全评估时不对人高功率演示 |

### 8.3 不应说的话

- “只有这个点能听到，旁边完全听不到”；
- “理论主瓣1.54°，所以实物就是1.54°”；
- “栅瓣固定-29.6 dB，肯定听不见”；
- “120 Vpp是换能器连续安全电压”；
- “相机和阵列同轴就完全免标定”；
- “ESP32官方有hpoint，所以8路相位肯定无毛刺”；
- “40 kHz听不见，所以绝对安全”；
- “SRAM一定把THD降到10%”。

把这些改成“理论估算/候选方法/将通过实测验证”。

---

## 9. 六分钟演示脚本

### 0:00～0:45：问题和原理

- 普通音箱扩散，定向讲解需要局部声区；
- 展示40 kHz波长与阵面照片；
- 一句话说明空气自解调。

### 0:45～1:45：架构

- 指出96阵元、16列独立接出；
- 拔下/展示8→16配线模块；
- 说明摄像头和平台不是依赖；
- 展示保险丝、总开关和驱动模块。

### 1:45～3:00：保底版实物

- 播放规定语句；
- 麦克风或两名体验者在轴上/侧面比较；
- 展示实测方向图，不用现场随机听感替代数据。

### 3:00～4:15：完整版功能（只有通过才展示）

- PC手工发送`-θ、0、+θ`；
- 显示声场峰值或预录同步测量；
- 展示一个焦区热力图；
- 视觉可用时只演示目标命令来源，视觉失败立即切手工。

### 4:15～5:15：数据和工程取舍

- 一张理论/仿真/实测方向图；
- 一张DSB/SRAM失真或频响图；
- 说明8路限制、栅瓣和16路升级结构。

### 5:15～6:00：安全和结论

- 说明低功率、静音、保险丝和测量；
- 总结保底/完整/进阶；
- 不用尚未完成的功能占用时间。

---

## 10. 现场回退

| 故障 | 立即切换 |
|---|---|
| PC控制断开 | ESP32本地内置语音 + FIXED |
| 摄像头/模型失败 | PC手工角度，或完全断开PC |
| 相位模式异常 | 全0相位保底版 |
| 某驱动通道异常 | 静音断电；若有预制旁路线束，降阵元演示；否则播放录像 |
| 谐振过热 | 拔掉电感，使用无谐振黄金设置 |
| 现场反射/噪声太大 | 缩短演示距离，展示预先测得方向图和同步录像 |
| 安全测量/现场条件不允许 | 不对人发射，展示低功率仪器演示和完整视频 |
| 整机无法启动 | 播放未剪辑的黄金版本演示视频并展示实物/数据 |

必须提前准备：保底版二进制、USB线、备用ESP32、备用TC4428A、保险丝、无谐振线束、内置语音、PPT内嵌视频和离线副本。

---

## 11. 最终交付检查表

```text
□ README/01～07 版本一致，无旧L0～L4/机械优先矛盾
□ 96阵元、16列和8路映射与实物标签一致
□ 电源有保险丝、总开关、星形分配
□ 默认/故障静音通过
□ 单通道和8路波形归档
□ 每列电容、每通道电感和最大设置有记录
□ 保底版不连接PC可运行
□ 方向图含原始数据、脚本、测量条件
□ 完整版相位/转向/焦区只有通过后才写入成绩
□ 超声安全评估和现场限制已确认
□ 固件、配置、源码、BOM、接线图、照片完整
□ 黄金版本已tag、bin已备份
□ 演示视频、PPT、报告有两份离线备份
□ 三位成员均能解释原理、架构、自己模块和回退方案
```

项目最有说服力的不是功能列表，而是：我们知道理论在哪里失效，知道每个模块怎样单测，知道失败时如何降级，并用数据说明样机实际做到了什么。
