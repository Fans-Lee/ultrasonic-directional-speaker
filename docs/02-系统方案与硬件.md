# 02 · 系统方案与硬件实施说明

> 本文件是硬件组、装配组和测试组的共同施工依据。
> 当前实现方式：洞洞板焊接，阵面板、驱动板、电源板彼此可拆；不以自制 PCB、摄像头和旋转平台为前置条件。

---

## 1. 整机硬件架构

```text
                         ┌──────── PC（可选）────────┐
                         │ 手工控制 / USB 摄像头识别 │
                         └──────────┬────────────────┘
                                    │ USB 串口，高层命令
┌─────────────── 低压逻辑区 ────────▼─────────────────────────┐
│ ESP32-S3 音频/相位控制板                                   │
│  ├─ 8 路 40 kHz 逻辑时序                                   │
│  ├─ 音频包络                                               │
│  └─ 默认静音/看门狗                                        │
│          │ 3.3 V 逻辑                                      │
│  可选：2×74AHCT125（5 V 供电，3.3→5 V 整形）               │
└──────────┬──────────────────────────────────────────────────┘
           │ 8 路逻辑 + GND
┌──────────▼────────── 12 V 驱动区 ───────────────────────────┐
│ 8 个独立 TC4428A 单元                                      │
│ 每单元：输入下拉、近端去耦、互补差分输出、电感旁路/插座     │
└──────────┬──────────────────────────────────────────────────┘
           │ 8 路差分输出
┌──────────▼────────── 可拆配线区 ────────────────────────────┐
│ 8 通道：每路并接相邻两列；16 通道升级时更换为一对一线束    │
└──────────┬──────────────────────────────────────────────────┘
           │ 16×A/B
┌──────────▼────────── 阵面区 ────────────────────────────────┐
│ 16 列 × 6 行 = 96 个 TCT40-16T；每列 6 个并联、独立接出    │
└─────────────────────────────────────────────────────────────┘

独立电源模块：12 V/5 A → DC5521 母座 → 5 A 保险丝 → 总开关
                                  → 星形分配到驱动板和 ESP32 支路
```

硬件边界：

- ESP32 换型，不需要改阵面；
- 8 路升级到 16 路，不需要拆换能器；
- 相位算法失败，8 路仍可全部同相完成保底版；
- 视觉程序失败，串口或本地按键仍能控制；
- 谐振效果不理想，可以拔掉电感跳线回到无谐振状态。

---

## 2. 阵面模块：16 列必须独立接出

### 2.1 物理排布

| 项目 | 设计值 | 必须实测/确认 |
|---|---|---|
| 换能器 | TCT40-16T 发射型 | 不得混入 R 接收型；TR 也不作为默认替代 |
| 数量 | 16×6=96 | 另留备件和筛选件 |
| 外径 | 标称约 16 mm | 卡尺抽测至少 10 个 |
| 引脚距 | 卖家可能给 5 mm 或其他值 | 洞洞板试插后冻结 |
| 洞洞板孔距 | 2.54 mm | 购买双面独立焊盘 FR-4 |
| 暂定中心距 | 7 孔距=17.78 mm | 若外壳/引脚不适配则修改 |
| 阵面板 | 约 20×30 cm | 先画 1:1 纸样放实物 |

建议先在纸上或空洞洞板标出 16×6 的中心点，插入 2×3 个换能器检查：

- 外壳之间是否留有装配间隙；
- 引脚是否正好落入孔位；
- 阵面四周是否还能留出固定孔和列连接器；
- 板材是否会因 96 个金属壳重量明显弯曲。

中心距一旦确定，测出每列中心 `x` 和每行中心 `y`，交给软件组写入几何配置。不要在软件里继续假设“16 mm”。

### 2.2 电气分组

每一列 6 个换能器同极性并联：

```text
COLn_A ──┬── T(n,0) ──┬── COLn_B
         ├── T(n,1) ──┤
         ├── T(n,2) ──┤
         ├── T(n,3) ──┤
         ├── T(n,4) ──┤
         └── T(n,5) ──┘
```

16 列一共引出 32 个端点：`COL0_A/B`～`COL15_A/B`。阵面背面禁止把相邻列永久焊在一起。

### 2.3 极性怎么处理

压电发射器没有像电解电容那样的直流正负供电，但多个阵元必须保持一致的机械相位。实物可能通过长脚、壳体标记或数据手册规定相位端。

流程：

1. 选一只作为参考；
2. 用同一 40 kHz 小信号分别激励两只，在相同位置用接收换能器/麦克风或双通道方法比较相位；
3. 确认外观标记与相位的对应；
4. 所有器件统一朝向插装，并在板上标 `A/B`；
5. 若无法可靠识别极性，至少保证同批次、同方向安装，再通过逐列声压比较找异常列。

不要用万用表二极管档臆测压电相位。

### 2.4 洞洞板背面布线

- 每列使用两条 22～24 AWG 母线；长距离裸铜线只允许同一网络连续使用；
- 母线交叉处必须换绝缘跳线；
- 焊点完成后剪去尖锐余脚，防止压到外壳或相邻线；
- 每列末端使用额定电流不低于 2 A 的 2 针锁紧连接器、螺钉端子或明确焊盘；
- 线束长度尽量一致且短，A/B 成对走线，减小回路面积；
- 每焊完一列就测通断和电容，记录到表中；
- 阵面背后可加绝缘板保护，不能让金属支架碰到母线。

最终成果不使用面包板或普通杜邦线承载驱动输出。杜邦线只允许用于低压逻辑临时验证。

### 2.5 阵面验收表

| 检查 | 方法 | 通过条件 |
|---|---|---|
| A/B 短路 | 万用表 | 所有列均无直流短路 |
| 列连续性 | 目检/万用表 | 6 个器件均接入，没有虚焊 |
| 列电容 | LCR/电容表 | 记录实值；异常列复查 |
| 列间隔离 | 相邻列测量 | 未通过阵面板永久连接 |
| 外观 | 照片 | 方向一致、编号可见、无裸线碰壳 |
| 机械 | 轻晃/固定 | 换能器不松动、板材不过度弯曲 |

---

## 3. 8→16 通道可拆配线模块

### 3.1 8 通道线束

差分输出的两根线都要并接：

| 驱动输出 | 阵面列 |
|---|---|
| `DRV0_A/B` | `COL0_A/B` + `COL1_A/B` |
| `DRV1_A/B` | `COL2_A/B` + `COL3_A/B` |
| `DRV2_A/B` | `COL4_A/B` + `COL5_A/B` |
| `DRV3_A/B` | `COL6_A/B` + `COL7_A/B` |
| `DRV4_A/B` | `COL8_A/B` + `COL9_A/B` |
| `DRV5_A/B` | `COL10_A/B` + `COL11_A/B` |
| `DRV6_A/B` | `COL12_A/B` + `COL13_A/B` |
| `DRV7_A/B` | `COL14_A/B` + `COL15_A/B` |

不能出现 `COLn_A` 接另一路的 `B`。配线板正反面都写清通道号，并制作一张可拍照核对的接线表。

### 3.2 16 通道线束

进阶版改为 `DRVn_A/B → COLn_A/B`。更换线束后：

- 每通道电容约减半；
- 谐振电感要重算，不能沿用 8 通道值；
- 软件通道映射和相位配置切换为 16；
- 所有 G4～G6 电气/声学测试重新执行。

---

## 4. TC4428A 单通道驱动

### 4.1 为什么一个 TC4428A 对应一个“通道”

TC4428A 内含两个推挽输出，其中一个输入路径反相、一个同相。把同一个逻辑信号送入两个输入，两个输出理想情况下相反：

```text
ESP_PWM ──┬── IN_A（反相路径）── OUT_A ──[可选 L/R]──┐
          └── IN_B（同相路径）── OUT_B ─────────────┤  换能器组
12 V/GND → TC4428A 供电                              └────────
```

换能器组接在 OUT_A 与 OUT_B 之间。12 V 供电时，理想差分方波接近 24 Vpp。TC4428A 是 MOSFET 栅极驱动器，在这里借用其高速推挽输出直接驱动压电负载；它不是专用音频功放，也不是带保护和死区管理的完整 H 桥模块。

[Microchip 官方资料](https://www.microchip.com/en-us/product/TC4428A)给出 4.5～18 V 供电、1.5 A 峰值输出、典型 7 Ω 输出电阻和 2.4 V 高电平门限。峰值电流指标不等于可以持续向任意大电容输出 1.5 A。

### 4.2 每个驱动单元必须包含

| 元件/接口 | 作用 | 布局要求 |
|---|---|---|
| TC4428AEPA DIP-8 | 双推挽互补驱动 | 使用插座；先核对丝印和官方引脚图 |
| 输入 A/B 同接 | 产生相反输出 | 逻辑线短；输入节点不得悬空 |
| 10 kΩ 下拉 | ESP32 复位时固定为低 | 靠近输入 |
| 100 nF 陶瓷 | 高频去耦 | 紧贴 VDD/GND，回路最短 |
| 4.7～10 µF | 局部储能 | 每芯片或每两芯片附近 |
| 电感/电阻插座 | 调谐、限流、可更换 | 同时提供 0 Ω/跳线旁路 |
| 2P 输出端 | 接配线模块 | 标记 A/B 和通道号 |
| 测试点 | OUT_A、OUT_B、VDD、GND | 便于安全测量 |

驱动板入口另加 470 µF/25 V 电解和 100 nF，电解注意极性。

### 4.3 3.3 V 逻辑和现有 125 系列芯片

TC4428A 的保证高电平门限为 2.4 V，ESP32-S3 的 3.3 V 输出在逻辑上可满足，但长线、地弹和干扰会减少裕量。

| 已有器件 | 能否用于本项目 | 说明 |
|---|---|---|
| `74AHCT125N` | **推荐** | 5 V 供电且 TTL 门限适合 3.3 V 输入；1 片 4 路，8 路共需 2 片 |
| `74HC125N` | 不推荐作 5 V 电平提升 | 5 V 供电时高电平门限通常过高，3.3 V 不保证可靠 |
| `74LVC125AD` | 可作 3.3 V 缓冲 | 3.3 V 供电时仍输出 3.3 V；是否允许特定引脚 5 V 容限看具体数据手册 |

建议步骤：

1. G1 单通道先用 ESP32 直驱，确认 TC4428 输入/输出；
2. 完整 8 路长期线束使用 2×74AHCT125；
3. AHCT 每片也要 100 nF 近端去耦；
4. OE 固定在明确使能状态或接入总静音，不能悬空；
5. 缓冲器只处理逻辑，不能代替 TC4428A。

### 4.4 差分输出的示波器测量安全

普通台式示波器各探头地夹通常与保护地相连。**不能把地夹接在 OUT_A 或 OUT_B 上**，否则可能直接把驱动输出短到地。

安全方法：

1. 两个探头地夹都接电路 GND；
2. CH1 探 OUT_A，CH2 探 OUT_B；
3. 示波器数学通道计算 `CH1-CH2`；
4. 或使用额定电压和带宽合适的差分探头；
5. 探头倍率、最大输入和接地方式由实验室老师确认。

---

## 5. 谐振网络：默认可旁路

### 5.1 选值起点

按单个换能器 `2.0～2.5 nF` 估算：

| 版本 | 每通道 | 估算 C | 40 kHz 理论 L | 第一批只买样品 |
|---|---:|---:|---:|---|
| 保底/完整 | 12 个 | 24～30 nF | 0.53～0.66 mH | 470/560/680/820 µH |
| 进阶 | 6 个 | 12～15 nF | 1.06～1.32 mH | 1.0/1.2/1.5 mH |

选电感还要看：

- 标称电感量和公差；
- 额定/饱和电流至少 0.5 A，实际按测量留余量；
- 40 kHz 下仍有合理电感量；
- DCR 尽量低，但过低可能形成过高 Q；
- 磁芯在 40 kHz 下的损耗和可闻啸叫；
- 体积、安装和绝缘。

### 5.2 调谐流程

```text
无电感旁路
  ↓ 单换能器波形正常
一列 6 个
  ↓ 波形/电流正常
两列 12 个
  ↓ 测 C_group
接入偏保守的 L
  ↓ 小范围扫频
记录 Vpp / 输入电流 / 温升 / 可听声
  ↓
决定标称值、是否串 10～22 Ω 阻尼、是否批量安装
```

所有扫频先低增益、短时间进行。TCT40-16T 卖家给出的高 Vpp 往往是规定脉冲条件，连续运行上限必须向供应商索取或通过保守试验确定。

---

## 6. 电源、保险丝、开关和线材

### 6.1 正规 12 V/5 A 适配器做什么

它把市电安全隔离并转换为稳定的 12 V 直流，是驱动板的主能量来源。应选择：

- 有厂家/型号和基本认证信息；
- 输出 12 V DC、额定 5 A；
- 具有短路、过流、过压和过温保护说明；
- DC5521（外径 5.5 mm、内径 2.1 mm），中心正；
- 导线和插头不会在 5 A 附近明显发热。

“DC5521”只说明机械尺寸，不代表任何母座、细线或开发板铜箔都能通过 5 A。

### 6.2 外置主电源链路

| 部件 | 建议规格 | 作用 |
|---|---|---|
| DC5521 母座/转螺钉端子 | 明确额定 ≥5 A | 接适配器，不反复焊线 |
| 保险丝座 | ATO/ATC，≥10 A/32 VDC | 可靠固定 5 A 保险丝 |
| 保险丝 | 5 A 快/中速汽车片式，备 2～3 个 | 短路时先熔断，保护线束和适配器 |
| 总开关 | 12 VDC 条件额定 ≥10 A | 物理急停和断电 |
| 主线 | 16 AWG 红/黑 | 适配器到星形分配 |
| 驱动支路 | 18 AWG | 星形点到驱动板 |
| 列/输出线 | 22～24 AWG，短线成对 | 驱动到阵面 |

不要只看开关外壳写的交流额定值；直流灭弧能力不同，优先购买明确标注 12/24 VDC 电流的产品。

### 6.3 星形分配

```text
主开关后节点
  ├─ 18 AWG → 驱动板 12 V/GND
  ├─ 独立支路 → ESP32 开发板输入（建议约 1 A 支路保护）
  └─ 可选 12→5 V DC-DC → AHCT/辅助器件
```

- 驱动板、ESP32 和缓冲器必须共地；
- 大电流回路不要先经过 ESP32 再去驱动板；
- 逻辑地与功率地在电源入口附近有低阻连接；
- 12 V 和 GND 线并行走，减小环路；
- 第一次通电使用限流实验电源更安全，确认后再接 5 A 适配器。

### 6.4 自制 ESP32-S3 开发板的使用原则

现有 3 块板中的一块用于音频/相位主控，第二块用于独立固件和时序实验，第三块作为备件或进阶扩展。

板载 DC5521 可以给开发板本身供电，但不作为 96 阵元主电流的中转接口。开发板手册中的电源轨描述必须结合原理图、具体连接器和铜箔能力理解，不能把“12 V/100 W”当成整条输入链路已验证可承受 100 W。

---

## 7. ESP32 逻辑接口和建议引脚

使用 ESP32-S3-WROOM-1-N16R8 时，八线 PSRAM 会占用部分 GPIO。最终引脚表必须同时核对：

1. 芯片/模组数据手册；
2. 自制开发板原理图和排针；
3. 板载 L298N、USB、启动脚等现有连接；
4. 实物通断测试。

保底候选 8 路：`GPIO4, 5, 6, 7, 15, 16, 17, 18`。这只是软件配置初值，硬件组必须确认它们确实引出、没有与板载器件冲突后签字冻结。

建议连接器：

| 针 | 信号 | 针 | 信号 |
|---:|---|---:|---|
| 1 | CH0 | 2 | GND |
| 3 | CH1 | 4 | GND |
| 5 | CH2 | 6 | GND |
| 7 | CH3 | 8 | GND |
| 9 | CH4 | 10 | GND |
| 11 | CH5 | 12 | GND |
| 13 | CH6 | 14 | GND |
| 15 | CH7 | 16 | GND |
| 17 | MUTE/OE（可选） | 18 | 5 V（若需要） |

逻辑排线每路旁安排地线有利于降低串扰。不要把 12 V 放进同一个易插反的逻辑排针。

---

## 8. 摄像头和旋转平台放在哪里

### 8.1 摄像头

保底版不买摄像头。完整版若要视觉控制，推荐普通 USB UVC 摄像头接电脑：

```text
USB 摄像头 → PC/OpenCV/目标检测 → 目标角度/坐标 → USB 串口 → ESP32
```

优势是模型和调试工具成熟、可录制视频复现、不会占用实时声学控制资源。普通单目相机只可靠提供图像角度；距离可先由固定演示区或手工输入，深度传感器放到进阶版。

### 8.2 旋转平台

当前不购买。只有出现以下需求时才进入进阶采购：

- 需要覆盖远大于电子小角度扫描的范围；
- 固定阵列无法满足现场多人分布；
- 保底版和完整版声学链路已经稳定；
- 团队有时间处理机械噪声、限位、供电和标定。

平台应转动整个阵面，不需要给 96 个换能器分别安装旋转基座。若加入平台，转动时自动静音，停稳后才恢复发声。

---

## 9. 硬件实施顺序

| 门 | 实物范围 | 本门只验证什么 | 通过后才能做 |
|---|---|---|---|
| G0 | 器件和空板 | 尺寸、引脚、电容、极性、适配器 | 焊接 |
| G1 | 1×TC4428A + 1 个换能器，无 L | 逻辑、互补输出、静音、温升 | 一列 |
| G2 | 1 列 6 个 | 并联负载、电容、极性 | 两列 |
| G3 | 2 列 12 个 | 当前通道负载和电感调谐 | 复制驱动 |
| G4 | 8 路驱动，不接全阵 | 8 路编号、波形、去耦、串扰 | 全阵 |
| G5 | 96 阵元同相 | 固定定向发声和稳定性 | 独立相位 |
| G6 | 8 路相位 | 转向/焦区热力图 | 视觉/16 路 |

每一门都保留照片、接线图、示波器截图、测量条件和结论。一次只改变一个变量。

---

## 10. 硬件组最终交付物

1. 阵面正反面高清照片和 16 列编号图；
2. 8 通道和 16 通道两张配线表；
3. 单通道原理图、洞洞板布局图和 TC4428A 官方引脚引用；
4. 电源树、保险丝/开关/线径表；
5. 每列电容、每通道电感和扫频结果；
6. GPIO、逻辑连接器、输出连接器针脚表；
7. G0～G5 的电压、电流、Vpp、温升和波形记录；
8. 最大已验证连续工作设置和明确禁止设置；
9. 备件位置和快速更换说明。

软件接口见 [03-软件架构.md](03-软件架构.md)，具体采购规格见 [06-采购清单.md](06-采购清单.md)，测量和排故见 [07-版本阶梯与风险.md](07-版本阶梯与风险.md)。
