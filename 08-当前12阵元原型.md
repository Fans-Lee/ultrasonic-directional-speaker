# 08 · 当前 4×3、12 阵元原型

> 状态日期：2026-08-13
> 本文件只描述已经搭建和正在测试的最小原型。96 阵元目标方案见 [README](README.md) 和 01～07 文档。

## 1. 当前原型能做什么

当前阵面为 4 列×3 行，共 12 个 TCT40-16T 发射换能器。每列 3 个并联，由 1 片 TC4428A 驱动；4 列分别由 ESP32-S3 的 GPIO4、GPIO5、GPIO6、GPIO7 控制。

```text
正面观察

 CH1     CH2     CH3     CH4
  ○       ○       ○       ○
  ○       ○       ○       ○
  ○       ○       ○       ○
GPIO4   GPIO5   GPIO6   GPIO7
```

当前固件支持：

- 单独开启 CH1～CH4；
- 四列同相输出 40 kHz 载波；
- 1 kHz 包络测试；
- 播放烧录在 Flash 中的测试旋律或 PCM WAV 音频；
- 串口立即停止输出。

它用于证明最小电气链路和实验方法，不代表已经实现 96 阵元整机、二维聚焦或经过校准的声压指标。

## 2. 当前接线摘要

每片 TC4428A 使用相同接法：

```text
对应 GPIO ──10～100 Ω──┬── pin 2 IN_A
                       ├── pin 4 IN_B
                       └──10 kΩ── GND

+12 V ───────────────────── pin 6 VDD
GND ─────────────────────── pin 3 GND

pin 6 ──100 nF── pin 3      每片芯片近端去耦
pin 6 ──10 µF─── pin 3      电解正极接 pin 6

pin 7 OUT_A ──10～22 Ω── 该列 3 只换能器的 A 脚
pin 5 OUT_B ──────────── 该列 3 只换能器的 B 脚
```

换能器接在 OUT_A 与 OUT_B 之间，两端均不能接 GND。四列的输出母线互不连接，只共用 12 V 和 GND。ESP32 GND、TC4428A pin 3 和 12 V 电源负极必须共地。

详细逐针说明、DC5521/Type-C 供电边界和安全上电顺序见 [最小原型接线说明](esp32_s3_4x3_min_test/README.md)。

## 3. 当前原型物资

| 物资 | 数量 | 用途 |
|---|---:|---|
| ESP32-S3 开发板 | 1 | 4 路 40 kHz 时序和音频调制 |
| TCT40-16T 发射换能器 | 12 | 4 列×3 行阵面 |
| TC4428AEPA、DIP-8 | 4 | 每列 1 片互补驱动 |
| DIP-8 插座 | 4 | 便于更换驱动器 |
| 10～100 Ω、1/4 W 电阻 | 4 | GPIO 输入串联；现有 10 Ω 可用 |
| 10 kΩ、1/4 W 电阻 | 4 | 输入下拉，防止上电悬空 |
| 100 nF、≥25 V 陶瓷电容 | 4 | 每片驱动器近端高频去耦 |
| 4.7～10 µF、≥25 V 电容 | 4 | 每片驱动器局部储能 |
| 470 µF、≥25 V 电解电容 | 1 | 驱动板 12 V 总入口储能 |
| 10～22 Ω、≥0.5 W 电阻 | 4 | 输出串联阻尼和初测保护 |
| 12 V 电源、配电及保护 | 1 套 | BLX-A/星形分配、开关和保险丝 |
| 洞洞板、18/22～24 AWG 线、端子 | 若干 | 装配和连接 |

该表是当前原型的设计清单。每次上电前仍应按实物逐项确认，不把表格自动视为“已安装”。96 阵元采购计划见 [06-采购清单.md](06-采购清单.md)。

## 4. 固件和播放方法

Arduino 工程位于 [`esp32_s3_4x3_min_test`](esp32_s3_4x3_min_test/)。`.ino` 与 `audio_data.h` 必须放在同一工程目录。

串口监视器设置为 115200 波特率：

| 命令 | 功能 |
|---|---|
| `1`～`4` | 单独开启对应列的 40 kHz 载波 |
| `A` | 四列同相输出 40 kHz |
| `T` | 40 kHz 载波加 1 kHz 正弦包络 |
| `P` | 播放一次 Flash 音频 |
| `L` | 循环播放 Flash 音频 |
| `0` | 停止切换输出 |
| `H` | 打印帮助 |

把自己的 PCM WAV 转换成固件头文件：

```powershell
py .\esp32_s3_4x3_min_test\wav_to_audio_header.py `
  .\input.wav `
  .\esp32_s3_4x3_min_test\audio_data.h
```

转换后需要重新编译和烧录。

## 5. 当前实验现象与严谨结论

初步手机分贝测试曾显示阵面正前方约 90 dB，稍微偏离后显示约 30～40 dB；但人耳在不同方向听到的可听声差异不明显。

这组手机数值不能作为校准声压：普通手机麦克风和分贝 App 通常不覆盖 40 kHz，超声可能经过麦克风非线性、自动增益或采样混叠形成错误读数。该现象最多只能作为“超声载波可能具有明显方向性”的定性线索。

当前接法把 TC4428A 的两个输入接到同一 GPIO，再通过改变 40 kHz 方波占空比实现包络调制。这可能在换能器差分端引入普通音频平均分量，使换能器、洞洞板或结构直接产生不定向的可听泄漏。因此现阶段结论应写为：

> 已完成 12 阵元发射链路，并观察到疑似明显的超声方向性；尚未用合适仪器证明定向可听声，也未完成声压标定。

下一阶段优先验证固定 40 kHz、1 kHz 包络和音频播放三种状态的频谱差异，再考虑把两个驱动输入拆开，改为保持 50% 占空比、通过相位差控制差分幅度的调制方式。

## 6. 安全和测量边界

- 不把手机 90 dB 读数用于判断超声暴露是否安全；
- 不把耳朵靠近阵面，首次测试只运行数秒并检查驱动器、电阻和线束温升；
- 示波器测差分输出时，两探头地夹都接电路 GND，再使用 `CH1-CH2`；地夹不能接 OUT_A 或 OUT_B；
- 改线前必须关闭 12 V。串口 `0` 不是物理断电；
- 最终方向图应固定距离、角度、信号、增益和仪器，并保留原始数据。
